---
const { platform, username } = Astro.props;
---

<a class='txt-link' href={`https://www.${platform}.com/${username}`}
  >{platform.charAt(0).toUpperCase() + platform.slice(1)}</a
>

<style>
  a {
    padding: 0 0.5rem;
    color: var(--txt-clr);
  }
</style>
